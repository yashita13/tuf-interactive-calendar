import { WallCalendar } from "@/components/WallCalendar";

export default function Home() {
  return (
    <main className="min-h-screen bg-[#e8ecef] flex items-center justify-center py-12 px-4 sm:px-8">
      <WallCalendar />
    </main>
  );
}
